"""Deterministic detection rules. Plain Python — never the LLM.

These functions take already-extracted structured facts (rows from tool output)
and emit draft Findings with citations. They DECIDE; the model only narrates.
"""
